function createUser() {
    user = {};
    user.Name = document.querySelector('input[name="Name"]');
    user.Email = document.querySelector('input[name="Email"]');
    user.Password = document.querySelector('input[name="Password"]');
}